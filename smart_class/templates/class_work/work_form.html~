{% extends 'base.html' %}

{% block title %}{% trans "Умный класс" %}{% endblock %}

{% block content %}

<div class="container mt-4">
  <div class="card">
    <div class="card-header">
      <h2 class="mb-0">{% if object %}{% trans "Редактировать" %}{% else %}{% trans "Создать" %}{% endif %} {% trans "класс" %}</h2>
    </div>
    <div class="card-body">
      <form method="post">
        {% csrf_token %}
        <div class="mb-3">
          {{ form.as_p }}
        </div>
        <div class="d-flex justify-content-between">
          <a href="{% url 'class_work:list' %}" class="btn btn-secondary">{% trans "Отмена" %}</a>
          <button type="submit" class="btn btn-success">{% trans "Сохранить" %}</button>
        </div>
      </form>
    </div>
  </div>
</div>

{% endblock %}
